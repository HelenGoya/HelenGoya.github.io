<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Calculadora de Factura</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 2em;
      max-width: 500px;
    }
    label {
      display: block;
      margin-top: 1em;
    }
    input {
      width: 100%;
      padding: 0.5em;
      margin-top: 0.3em;
    }
    .resultado {
      margin-top: 2em;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <h2>Calculadora de Factura</h2>

  <label for="base">Base imponible (€):</label>
  <input type="number" id="base" step="0.01" value="0">

  <label for="iva">IVA (%):</label>
  <input type="number" id="iva" step="0.01" value="21">

  <label for="irpf">Retención IRPF (%):</label>
  <input type="number" id="irpf" step="0.01" value="15">

  <button onclick="calcularFactura()">Calcular</button>

  <div class="resultado" id="resultado"></div>

  <script>
    function calcularFactura() {
      const base = parseFloat(document.getElementById('base').value) || 0;
      const iva = parseFloat(document.getElementById('iva').value) || 0;
      const irpf = parseFloat(document.getElementById('irpf').value) || 0;

      const importeIVA = (base * iva) / 100;
      const retencionIRPF = (base * irpf) / 100;
      const total = base + importeIVA - retencionIRPF;

      document.getElementById('resultado').innerHTML = `
        <p>🧾 Base imponible: <strong>${base.toFixed(2)} €</strong></p>
        <p>💸 IVA (${iva}%): <strong>${importeIVA.toFixed(2)} €</strong></p>
        <p>📉 Retención IRPF (${irpf}%): <strong>- ${retencionIRPF.toFixed(2)} €</strong></p>
        <p>💰 <u>Total factura:</u> <strong>${total.toFixed(2)} €</strong></p>
      `;
    }
  </script>

</body>
</html>
